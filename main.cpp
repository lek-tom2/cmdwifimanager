/*

booting up app
checks:
if wifi is tured on
what wlan interface are you raunning

commands:

list - lists aviable wifis
list -d lists aviable wifis with thier details
connect(name || ip || etc)
disconnect(name || ip || etc)

app will run continously printing nev wifis scanning every minute
*/

#include <iostream>

int main(int argc, char** argv) {
    return 0;
}
